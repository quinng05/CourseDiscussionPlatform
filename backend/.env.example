PORT=3000
SESSION_SECRET=use-a-long-random-string-in-production
CLIENT_ORIGIN=http://localhost:5173
# Optional — omit to use in-memory mock data for forums/posts (login still accepts any credentials).
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=coursediscussionplatform