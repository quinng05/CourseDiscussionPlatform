# Written reports

Place course or project **written reports** (for example exported PDFs) in this directory so they stay next to the codebase and are easy to find for submission or grading.

The in-app **Reports** page (`/reports`) shows **live SQL aggregates** from the database; it is separate from files stored here.
